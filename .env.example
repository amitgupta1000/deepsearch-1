# INTELLISEARCH API Key Configuration
# Copy this file to .env and set your production API keys

# =============================================================================
# INTELLISEARCH ACCESS CONTROL
# =============================================================================

# Format: INTELLISEARCH_API_KEY_N=api_key:user_id
# Replace with your own secure API keys in production

INTELLISEARCH_API_KEY_1=demo-key-research-123:demo_user
INTELLISEARCH_API_KEY_2=demo-key-admin-456:admin_user
INTELLISEARCH_API_KEY_3=prod-key-team-alpha:team_alpha
INTELLISEARCH_API_KEY_4=prod-key-team-beta:team_beta

# Admin users (comma-separated list)
ADMIN_USERS=admin_user,admin

# Rate limiting configuration
RATE_LIMIT_HOURLY=20
RATE_LIMIT_MINUTE=5

# =============================================================================
# EXISTING INTELLISEARCH CONFIGURATION
# =============================================================================

# Google API Key for Gemini AI
GOOGLE_API_KEY=your_google_api_key_here

# Serper API Key for web search
SERPER_API_KEY=your_serper_api_key_here

# Environment setting
ENVIRONMENT=development

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================

# 1. Generate secure API keys:
#    Use: openssl rand -base64 32
#    Or: python -c "import secrets; print('intellisearch-' + secrets.token_urlsafe(32))"

# 2. Set unique user IDs:
#    Format: company_department_user (e.g., acme_research_john)

# 3. Configure rate limits based on your needs:
#    - RATE_LIMIT_HOURLY: Max requests per hour per API key
#    - RATE_LIMIT_MINUTE: Max requests per minute per API key

# 4. For Render.com deployment:
#    Set these as environment variables in your Render dashboard
#    Do not commit real API keys to version control